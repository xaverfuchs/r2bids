% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/check_participant_data.R
\name{check_participant_data}
\alias{check_participant_data}
\title{Check and adjust the participant data before writing participants.tsv file}
\usage{
check_participant_data(
  data,
  participant_col = "participant_id",
  sex_col = NULL,
  age_col = NULL,
  ignore_cols = NULL
)
}
\arguments{
\item{data}{A data frame that contains the participant data to be bids-converted.}

\item{participant_col}{Name of the column containing participant IDs (default is "participant_id").}

\item{sex_col}{Name of the column containing sex information, if any (default is NULL, assuming there is no sex column).}

\item{ignore_cols}{Character vector with variable names to ignore. Sometimes the function will not rename the data in a desirable way (for example when special characters DO have a meaning in a factor). In this case you can ignore variables and rename/recode them manually instead.}
}
\value{
A checked and possibly adjusted data frame with the appropriate formatting.
}
\description{
This function checks that certain requirements are met by the data set, including variable names in snake_case,
and valid participant IDs description. The function can optionally also check for proper sex encoding ("m", "f", or "o"). It attempts to convert invalid data and
outputs warnings or errors if issues arise.
}
\examples{
# Example dataset
example_data <- data.frame(ParticipantID = c(1, 2), sex = c("Male", "Female"), Age = c(24, 45))
check_participant_data(data = example_data, participant_col = "ParticipantID", sex_col = "sex")
# Another example with an alphanumeric participant id
example_data2 <- data.frame(participant_id = c("ab123", "sub-123C3"), WeirdVar=c(NA, -666))
check_participant_data(data = example_data2, ignore_cols="WeirdVar")
}
