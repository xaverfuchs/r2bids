% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/check_task_data.R
\name{check_task_data}
\alias{check_task_data}
\title{Check and adjust the task data before writing task.tsv file}
\usage{
check_task_data(
  data,
  participant_col = "participant_id",
  session_col = "session",
  ignore_cols = NULL
)
}
\arguments{
\item{data}{A data frame that contains the data set with the (behavioral) task data to be checked.}

\item{participant_col}{Name of the column containing participant IDs (default is "participant_id").}

\item{session_col}{Name of the column containing session IDs (default is "session").}

\item{ignore_cols}{Character vector with variable names to ignore. Sometimes the function will not rename the data in a desirable way (for example when special characters DO have a meaning in a factor). In this case you can ignore variables and rename/recode them manually instead.}
}
\value{
A checked and possibly adjusted data frame with the appropriate formatting.
}
\description{
This function checks that certain requirements are met by the data set, including variable names in snake_case,
valid participant IDs, and session description. It attempts to convert invalid data and
outputs warnings or errors if issues arise.
}
\examples{
# Example dataset
example_data <- data.frame(ParticipantID = c(1, 2), Bad_Factor=c(NA, ""), Session = c(1, 1), RT = c(435, 876), weird_variable=c("1_2", "3_4"))
check_task_data(data = example_data, participant_col = "ParticipantID", session_col = "Session", ignore_cols=c("weird_variable"))
}
