% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/write_bids.R
\name{write_bids}
\alias{write_bids}
\title{Create BIDS-compliant behavioral data files}
\usage{
write_bids(
  data,
  output_dir,
  task_name,
  participant_info_cols,
  file_suffix = "beh"
)
}
\arguments{
\item{data}{A data frame containing the participant information and behavioral data.}

\item{output_dir}{The directory where the BIDS dataset will be saved.}

\item{task_name}{Name of the task, which will be used in the filename.}

\item{participant_info_cols}{A character vector of column names to include in the \code{participants.tsv} file. These variables will now be included in the tsv files containing the task data.}

\item{file_suffix}{A string defining the suffix for the task files. In BIDS it is usually "events" or "beh" (the default) for "behavior"}
}
\value{
This function does not return anything but writes files to the output directory.
}
\description{
This function creates BIDS-compliant behavioral data files in \code{.tsv} format.
It generates a participants file (\code{participants.tsv}) and individual
task-based files for each participant and session.
}
\examples{
data <- data.frame(participant_id = c(1, 1, 2, 2),
                   session = c(1, 2, 1, 2),
                   age = c(25, 25, 30, 30),
                   gender = c('m', 'm', 'f', 'f'),
                   response_time = c(100, 200, 150, 180))
write_bids(data, output_dir = "bids_dir", task_name = "RTTask", participant_info_cols = c("age", "gender"))

}
