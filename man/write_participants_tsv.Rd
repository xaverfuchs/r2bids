% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/write_participants_tsv.R
\name{write_participants_tsv}
\alias{write_participants_tsv}
\title{Create BIDS-compliant behavioral data files}
\usage{
write_participants_tsv(
  data,
  bids_dir,
  data_type = "participants",
  include_variables = c("participant_id")
)
}
\arguments{
\item{data}{A data frame containing the participant information.}

\item{bids_dir}{The directory where the BIDS data set will be saved.}

\item{data_type}{A string defining the data_type for the task files. Default is "participants" which will write a participants.tsv. You could however also repurpose this function to write another tsv file to contain information, for example a "session.tsv".}

\item{include_variables.}{A character vector with the variables that shluld make it into the file. Default is c("participant_id") but in reality it would rather be something like c("participant_id", "age", "sex").}
}
\value{
This function does not return anything but writes files to the output directory.
}
\description{
This function  generates a participants file (\code{participants.tsv}) or another kind of tsv with information in tabular format.
}
\examples{
data <- data.frame(participant_id = c("sub-1", "sub-1", "sub-2", "sub-2"),
                   session = c("ses-1", "ses-2", "ses-1", "ses-2"),
                   run = c("run-1", "run-1", "run-1", "run-1"),
                   age = c(25, 25, 30, 30),
                   sex = c('m', 'm', 'f', 'f'),
                   response_time = c(100, 200, 150, 180))
write_participants_tsv(data, bids_dir = "BIDS", include_variables = c("participant_id", "age", "sex"))
}
